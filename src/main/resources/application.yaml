server:
  port: 8081
auth0:
    audience: https://www.brooks-dusura.uk/api
spring:
  application:
    name: REFLECT_PLACES_1
  datasource:
    url: jdbc:postgresql://34.72.193.230:5432/postgres
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-4zduxht0r6gq1f7f.us.auth0.com/
  # ეს არის ავტორიზაციის სერვერის მისამართი სადაც არის კლიენტის ინფორმაცია, ამით ხდება კლიენტის ვალიდაცია, ავტორიზაციის სერვერისთვის ამ შემთხვევაში კლიენ
logging:
  level:
    org:
      springframework: info
      security:

        oauth2: debug

    root: info
    com:
      boris:
        reflect_places_1:
          config: debug
          security: debug
